[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
markers =
    integration: mark test as integration test (requires real API key)
    eval: mark test as eval test (slow, weekly CI only)
